# Objective

Normalize `data/profile/base/resume.raw.md` into canonical candidate data.

# Rules

- Preserve original facts.
- Use `unknown`, `not_provided` or `requires_user_confirmation` for gaps.
- Do not promote LinkedIn activity into professional evidence.