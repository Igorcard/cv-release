# Objective

Match candidate evidence against normalized job requirements.

# Rules

- Use only selected context chunks and skill evidence.
- Include source_ids for every match.
- Return valid JSON matching `schemas/match-analysis.schema.json`.